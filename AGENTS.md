# AI Passport 项目规则

## 代码归属

- 用户于 2026-09-12 指定项目代码统一存入 `https://github.com/xibumianbao/ai-passport.git`，这是本项目的代码仓库。
- 以当前 Git 仓库根目录为项目根目录。固件、服务端、Agent 适配、开发脚本和测试均应纳入该仓库的版本管理。
- 开发前检查 Git 状态、origin、分支和已有改动；使用 `codex/*` 开发分支，验证后推送，通过 PR 合并。保护其他任务正在修改的内容，明确列出每次暂存的文件。
- `reference/ai-passport` 是 FoloToy 官方源码研究副本。将选定代码引入项目时保留上游来源、版本和许可证。
- 仅把经过检查的项目源代码、必要开发文档和脱敏示例配置提交到 Git；凭据、个人 OB 笔记及其备份、原始接口响应和本地环境信息不提交。

## 项目记录

- 维护者的 OB 项目入口为其本地 vault 下的 `🧿Agent-Codex/project/ai passport/ai passport.md`；vault 绝对路径由本地启动规则提供，不存入公开仓库。
- 项目工作前回读总览、近期过程和相关决定；阶段结束后将进展、取舍、成果版本/提交、验证结果、待办和下一步写回该项目空间，并回读核验。
- 延续已确认的独立联网远程语音 Agent 方向；相关历史决定以 OB 原文和用户最新指令为准。
